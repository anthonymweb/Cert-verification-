<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Certificate Creator</title>
  <style>
    body { font-family: sans-serif; background: #f9f9f9; padding: 2rem; max-width: 600px; margin: auto; }
    form { background: white; padding: 1.5rem; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
    input, button { width: 100%; margin-top: 0.8rem; padding: 0.6rem; font-size: 1rem; }
    button { background: crimson; color: white; border: none; cursor: pointer; }
    button:hover { background: darkred; }
    .success, .error { margin-top: 1rem; padding: 1rem; border-radius: 6px; }
    .success { background: #e6ffed; color: green; }
    .error { background: #ffe6e6; color: crimson; }
    img { max-width: 100%; margin-top: 1rem; }
  </style>
</head>
<body>
  <h2>Admin: Create Certificate</h2>
  <form method="POST" action="/admin">
    <input type="text" name="firstName" placeholder="First Name" required />
    <input type="text" name="lastName" placeholder="Last Name" required />
    <input type="text" name="Training" placeholder="Training Program" required />
    <input type="date" name="Issueddate" required />
    <input type="text" name="certId" placeholder="Certificate ID" required />
    <button type="submit">Create Certificate</button>
  </form>

  <% if (success) { %>
    <div class="success">
      ✅ Certificate created for <%= success.cert.firstName %> <%= success.cert.lastName %><br>
      Cert ID: <%= success.cert.certId %><br>
      Course: <%= success.cert.course %><br>
      Date: <%= success.cert.Issueddate %><br>
      <p>🔗 <a href="<%= success.qrLink %>" target="_blank">Verify Link</a></p>
      <img src="<%= success.qrImage %>" alt="QR Code">
    </div>
  <% } %>

  <% if (error) { %>
    <div class="error"><%= error %></div>
  <% } %>
</body>
</html>