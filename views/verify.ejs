    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
      <title>Certificate Verification</title>
      <style>
        :root {
          --primary-color: #b30000;
          --bg-color: #ffffff;
          --text-color: #333;
          --error-color: #cc0000;
          --valid-color: #006400;
        }

        body {
          background-color: var(--bg-color);
          font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
          padding: 3rem 1rem;
          margin: 0;
          color: var(--text-color);
        }

        .container {
          max-width: 600px;
          margin: auto;
        }

        h2 {
          text-align: center;
          color: var(--primary-color);
          margin-bottom: 2rem;
        }

        form {
          display: flex;
          justify-content: center;
          margin-bottom: 2.5rem;
        }

        input[type="text"] {
          padding: 0.8rem;
          width: 70%;
          border: 1px solid #ccc;
          border-radius: 4px 0 0 4px;
          font-size: 1rem;
        }

        button {
          padding: 0.8rem 1.2rem;
          border: none;
          background-color: var(--primary-color);
          color: white;
          font-weight: bold;
          border-radius: 0 4px 4px 0;
          cursor: pointer;
          transition: background 0.3s;
        }

        button:hover {
          background-color: #990000;
        }

        .card {
          background-color: #fff;
          border: 1px solid #ddd;
          border-left: 6px solid var(--primary-color);
          border-radius: 6px;
          box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
          padding: 1.5rem;
          margin-top: 1rem;
        }

        .verified {
          color: var(--valid-color);
          font-weight: bold;
          font-size: 1.2rem;
          margin-bottom: 1rem;
        }

        .error {
          background-color: #ffe6e6;
          border: 1px solid var(--error-color);
          color: var(--error-color);
          padding: 1rem;
          border-radius: 6px;
          text-align: center;
          margin-top: 1.5rem;
        }

        .label {
          font-weight: bold;
        }

        .footer {
          text-align: center;
          font-size: 0.85rem;
          color: #aaa;
          margin-top: 3rem;
        }

      </style>
    </head>
    <body>
      <div class="container">
        <h2>🎓 Certificate Verification Portal</h2>

        <form method="POST" action="/verify">
          <input type="text" name="certId" placeholder="Enter Certificate ID" required />
          <button type="submit">Verify</button>
        </form>

        <% if (cert) { %>
          <div class="card">
            <div class="verified">✅ Certificate Verified</div>
            <p><span class="label">Name:</span> <%= cert.firstName %> <%= cert.lastName %></p>
            <p><span class="label">Training:</span> <%= cert.course %></p>
            <p><span class="label">Issued Date:</span> <%= cert.Issueddate %></p>
            <p><span class="label">Certificate ID:</span> <%= cert.certId %></p>
          </div>
        <% } else if (error) { %>
          <div class="error"><%= error %></div>
        <% } %>

        <div class="footer">© <%= new Date().getFullYear() %> Scratch and Script Limited. All rights reserved.</div>
      </div>
    </body>
    </html>